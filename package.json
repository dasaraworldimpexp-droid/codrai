{
  "name": "codrai",
  "version": "1.0.0",
  "private": true,
  "description": "CODRAI full-stack AI SaaS platform",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend -- --host 0.0.0.0 --open\"",
    "dev:frontend": "npm run dev --prefix frontend -- --host 0.0.0.0 --open",
    "dev:backend": "npm run dev --prefix backend",
    "install:all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
